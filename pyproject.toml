[project]
name = "callback"
version = "0.1.0"
description = "Vendor payment-change fraud interceptor"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "httpx>=0.27",
    "python-dotenv>=1.0",
    "python-multipart>=0.0.9",
]

[tool.uv]
package = false
